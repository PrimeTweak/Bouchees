{
  "build": 117,
  "date": "2026-09-01",
  "carries": [
    "three switches over the verdict status still missed the caution case and broke the build",
    "they sat below the point where a truncated search stopped, and the search was treated as complete",
    "check-swift now refuses a switch that misses a case of the enum it walks, with no default",
    "the rule reads every case in a clause, because case .a, .b: declares two",
    "comments are blanked rather than removed so a reported line matches the file, verified against the compiler's own line 962"
  ]
}
