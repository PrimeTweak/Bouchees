{
  "build": 160,
  "date": "2026-09-03",
  "carries": [
    "the scroll needed two or three tries after a swipe: highPriorityGesture claimed every drag on all fourteen rows ahead of the ScrollView, so a scroll had to fail the horizontal test before the list ever saw it; the gesture is ordinary now and only takes movement that is twice as horizontal as vertical",
    "the row's Button is silenced while the row is off its rest position, which is what kept a swipe from firing a tap without the gesture needing priority",
    "recipeByID and resultFor are indexed, and the swipe field is drawn only while a row travels"
  ]
}
