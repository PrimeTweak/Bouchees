{
  "build": 192,
  "date": "2026-09-07",
  "carries": [
    "APP STORE. Apple's four subscription items are in the paywall (title, length, StoreKit price, Terms + Privacy links) with Restore; the StoreKit test configuration no longer ships in the bundle; the privacy manifest declares UserDefaults (CA92.1) and no collected data — the app has no sign-in screen; camera usage string in both languages; ATS default; iPhone only",
    "LEGAL. Four TheMealDB/Spoonacular recipes are withheld — not deleted: data/withheld/, images/withheld/, ingest/withheld/ — and publish prunes their bodies from dist/. Terms and Privacy exist in French and English, served by language (/fr, /en, Accept-Language), with refunds via Apple, Québec governing law, the Law 25 person in charge, and the ODbL line. The contact address is BOUCHEES_CONTACT, the officer BOUCHEES_PRIVACY_OFFICER",
    "DATA. Session tokens are stored hashed; ratings are keyed on a hash of the account; the iOS token lives in the keychain (migrated once from UserDefaults); profiles.json has complete file protection",
    "ACCESS. Email bounded to 254 characters, five sign-in attempts a minute per address, Stripe answers 401 without naming the variable, checkout answers 503 without listing what is missing",
    "QUALITY. priceLine has one source (Subscription); BackButton is one component; every eyebrow uses .eyebrow(); SearchScreen and ChildPickerSheet live under Screens/",
    "from 190/191: single-ingredient labels get a verdict, the shopping aisle folds at the last tick, concurrent scans of one code share one lookup",
    "not done, on purpose: splitting Onboarding, Scanner, AppState and the tests — after publication, one file per build"
  ]
}
