{
  "build": 132,
  "date": "2026-09-02",
  "carries": [
    "cumulative from 128: 129, 130, 131 and this",
    "eight generated recipes carried a source keyed name instead of source; the Swift decoder failed on them and, the catalogue being one array, dropped all thirty-eight: no hero, seven empty days, an empty list, 0 today, and the Offline chip, since the server's catalogue failed the same way",
    "the data key is corrected; a malformed source is now a lost credit, never a lost recipe; every recipe array decodes lossily, so one bad card never takes the others down; check-decoding compares RecipeSource against every source shape in the corpus, which it never did",
    "the duplicate-type rule counts top-level declarations only; a CodingKeys nested in each Codable struct is the language's own pattern"
  ]
}
