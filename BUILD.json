{
  "build": 143,
  "date": "2026-09-02",
  "carries": [
    "build fix: the Apple receipt fallback used ?? whose right side is an autoclosure, which cannot await; the fallback is spelled out and the checker refuses await inside ?? or assert"
  ]
}
