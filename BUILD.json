{
  "build": 170,
  "date": "2026-09-03",
  "carries": [
    "PROBE BUILD. Seven fixes were reasoned from the code because SwiftUI cannot be run here; this one measures instead. Every step of a drop writes a line on screen: the lift, each hover, what dropDestination receives, whether the id resolves, and the plan's day counts before and after the move",
    "the probe panel and every sonde call come out in the next build"
  ]
}
