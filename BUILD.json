{
  "build": 158,
  "date": "2026-09-03",
  "carries": [
    "cooked was a computed property that read UserDefaults twice per row per frame — about seventeen hundred reads a second while scrolling, which is the freeze — and no write told SwiftUI to redraw, which is why a tick needed a restart; it is held in memory, observed, and reloaded with the plan so a week rollover still refreshes it",
    "the swipe field was rebuilt at every pixel with a frame driven by the offset; it is built once and revealed, and the offset itself is never animated because the value is the finger"
  ]
}
