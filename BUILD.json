{
  "build": 128,
  "date": "2026-09-02",
  "carries": [
    "the cycle counted published photos in dist/batches, which no longer exists; it reads the catalogue",
    "variable-only French identifiers renamed in the pipeline tools; the words that double as data keys stay as the data has them",
    "the probe's usage header fits the three-line rule",
    "the two workflow files and the weeks.js removal that the Finder hid on the last apply"
  ]
}
