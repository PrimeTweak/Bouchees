{
  "build": 137,
  "date": "2026-09-02",
  "carries": [
    "build fix: scaledFont(spec, design:) had no signature, so two monospace quantities fell to the CGFloat overload and failed to compile; the overload exists and the checker refuses a call shape no signature takes",
    "the two warnings Xcode raised: a @ViewBuilder over a body that returns explicitly, and a main-actor static read from a nonisolated delegate; both fixed, the first with a rule"
  ]
}
