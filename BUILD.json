{
  "build": 122,
  "date": "2026-09-01",
  "carries": [
    "the budget test stubs the product database instead of relying on every fetch failing: it was written offline and broke on a runner with a network, where the first call could succeed and end the plan early",
    "the stub also asserts that every step of the plan is tried on a miss, which the offline version could not"
  ]
}
